<%= form_for(@lenguaje) do |f| %>
  <% if @lenguaje.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lenguaje.errors.count, "error") %> prohibited this lenguaje from being saved:</h2>

      <ul>
      <% @lenguaje.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br />
    <%= f.text_field :nombre %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
